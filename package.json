{
  "name": "static-site",
  "version": "1.0.0",
  "description": "A simple static site with Markdown support",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && http-server ."
  },
  "dependencies": {
    "marked": "^9.1.5",
    "front-matter": "^4.0.2",
    "fs-extra": "^11.1.1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
} 